{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for claude-os shell (Phase 6a baseline).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
